export { makeRoomId, playerColor, encodeMsg, decodeMsg } from '../../src/multiplayer/protocol';
export type {
  ClientMessage,
  ServerMessage,
  WirePiece,
  WirePuzzleConfig,
  PlayerInfo,
  RoomSnapshot,
  RoomPhase,
} from '../../src/multiplayer/protocol';
